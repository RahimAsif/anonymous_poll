services:
  - type: web
    name: anonymous-poll
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000
    plan: free
    autoDeploy: true
